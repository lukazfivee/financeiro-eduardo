{
  "name": "financeiro-eduardo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check public/app.js && node --check public/sw.js",
    "db:local": "wrangler d1 execute financeiro-eduardo --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute financeiro-eduardo --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  }
}
